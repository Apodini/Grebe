name: Update Hombrew Tap
on: [push]
jobs:
  deployTap:
    runs-on: macOS-latest
    steps:
    - name: Update Homebrew formula
      uses: dawidd6/action-homebrew-bump-formula@v2
      with:
        token: ${{ secrets.HOMEBREW }}
        formula: Apodini/homebrew-tap/grebe.rb
        force: false
